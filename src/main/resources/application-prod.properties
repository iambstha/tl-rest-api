# DataSource settings

#Using postgres database
#spring.datasource.platform=postgres
#spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/tl_db}
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgresSQLDialect

#Using mysql database
spring.datasource.platform=mysql
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://mysql:3306/tl_db}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.datasource.username=${SPRING_DATASOURCE_USERNAME:bishal}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:shrestha}

jwt.private-key-path=app/keys/private_key.pem
jwt.public-key-path=app/keys/public_key.pem

server.error.include-message=always
server.error.include-binding-errors=always
